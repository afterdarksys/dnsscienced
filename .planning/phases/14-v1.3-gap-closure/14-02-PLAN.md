---
phase: 14-v1.3-gap-closure
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - .planning/phases/10-record-type-expansion/10-VERIFICATION.md
  - .planning/phases/10-record-type-expansion/10-VALIDATION.md
  - .planning/phases/11-resolver-behaviors/11-VALIDATION.md
autonomous: true
requirements: [RRTYPE-01, RRTYPE-02, RESOLVE-01, RESOLVE-02, RESOLVE-03]
must_haves:
  truths:
    - "Phase 10 VERIFICATION.md shows passed status, not human_needed"
    - "Phase 10 VALIDATION.md test commands match actual test function names and all rows show green"
    - "Phase 11 VALIDATION.md test commands match actual test function names and all rows show green"
    - "Phase 10 VALIDATION.md has nyquist_compliant: true in frontmatter"
    - "Phase 11 VALIDATION.md has nyquist_compliant: true in frontmatter"
  artifacts:
    - path: ".planning/phases/10-record-type-expansion/10-VERIFICATION.md"
      provides: "Updated verification reflecting Plan 03 results"
      contains: "passed"
    - path: ".planning/phases/10-record-type-expansion/10-VALIDATION.md"
      provides: "Corrected test commands and green status"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/11-resolver-behaviors/11-VALIDATION.md"
      provides: "Corrected test commands and green status"
      contains: "nyquist_compliant: true"
  key_links:
    - from: ".planning/phases/10-record-type-expansion/10-VALIDATION.md"
      to: "internal/zone/*_test.go"
      via: "go test -run commands"
      pattern: "TestParseDNSZone_|TestParseBIND_|TestRoundTrip_"
    - from: ".planning/phases/11-resolver-behaviors/11-VALIDATION.md"
      to: "internal/resolver/*_test.go"
      via: "go test -run commands"
      pattern: "TestServeStale_BeyondMaxStaleTTL"
---

<objective>
Re-verify Phase 10, fix VALIDATION.md test name mismatches for phases 10 and 11, and achieve Nyquist compliance.

Purpose: Phase 10 VERIFICATION.md is stale (predates Plan 03 which added TLSA/HTTPS/SVCB tests). VALIDATION.md files for phases 10 and 11 have test patterns that don't match actual test function names, preventing Nyquist compliance.

Output: Updated VERIFICATION.md (passed), corrected VALIDATION.md files with green status and nyquist_compliant: true.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-v1.3-gap-closure/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify Phase 10 and update VERIFICATION.md (per D-06, D-07)</name>
  <read_first>
    - .planning/phases/10-record-type-expansion/10-VERIFICATION.md (current stale state)
    - .planning/phases/10-record-type-expansion/10-03-SUMMARY.md (Plan 03 results confirming RRTYPE-01/02)
  </read_first>
  <files>.planning/phases/10-record-type-expansion/10-VERIFICATION.md</files>
  <action>
Run the Phase 10 test suite to confirm all tests pass:

```bash
go test ./internal/zone/... -count=1 -v 2>&1 | head -100
```

Then update `10-VERIFICATION.md` to reflect the complete Phase 10 state:

1. Change the status in frontmatter from `human_needed` to `passed`
2. Record that RRTYPE-01 (HTTPS/SVCB) is satisfied — confirmed by TestParseBIND_HTTPS, TestParseBIND_SVCB, TestParseDNSZone_HTTPS, TestParseDNSZone_SVCB, TestRoundTrip_HTTPS, TestRoundTrip_SVCB (per D-07)
3. Record that RRTYPE-02 (TLSA) is satisfied — confirmed by TestParseBIND_TLSA, TestParseDNSZone_TLSA, TestRoundTrip_TLSA (per D-07)
4. Record that RRTYPE-03 through RRTYPE-08 are satisfied (existing tests from Plans 01/02)
5. Include the date of re-verification and note that this supersedes the prior `human_needed` status which predated Plan 03

The VERIFICATION.md format should include:
- Frontmatter with `status: passed`
- A requirements satisfaction table mapping each RRTYPE-0X to its confirming tests
- The `go test` command output summary (pass count)
- Date of verification
  </action>
  <verify>
    <automated>cd /Users/ryan/development/dnsscienced && go test ./internal/zone/... -count=1 && grep -c 'passed' .planning/phases/10-record-type-expansion/10-VERIFICATION.md | grep -q '[1-9]' && echo "PASS: Phase 10 re-verified"</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/zone/... -count=1` exits 0
    - `grep 'status: passed' .planning/phases/10-record-type-expansion/10-VERIFICATION.md` returns 1 match
    - `grep 'human_needed' .planning/phases/10-record-type-expansion/10-VERIFICATION.md` returns 0 matches
    - `grep 'RRTYPE-01' .planning/phases/10-record-type-expansion/10-VERIFICATION.md` returns at least 1 match
    - `grep 'RRTYPE-02' .planning/phases/10-record-type-expansion/10-VERIFICATION.md` returns at least 1 match
  </acceptance_criteria>
  <done>Phase 10 VERIFICATION.md shows passed; RRTYPE-01 and RRTYPE-02 marked satisfied with confirming test names</done>
</task>

<task type="auto">
  <name>Task 2: Fix VALIDATION.md test name mismatches and achieve Nyquist compliance (per D-09, D-10)</name>
  <read_first>
    - .planning/phases/10-record-type-expansion/10-VALIDATION.md (current stale rows with wrong test names)
    - .planning/phases/11-resolver-behaviors/11-VALIDATION.md (current stale rows with one wrong test name)
  </read_first>
  <files>.planning/phases/10-record-type-expansion/10-VALIDATION.md, .planning/phases/11-resolver-behaviors/11-VALIDATION.md</files>
  <action>
**Phase 10 VALIDATION.md fixes:**

First, run each corrected test command to confirm they pass:
```bash
go test ./internal/zone/... -run 'TestParseDNSZone_SSHFP|TestParseBIND_SSHFP|TestRoundTrip_SSHFP' -count=1
go test ./internal/zone/... -run 'TestParseDNSZone_NAPTR|TestParseBIND_NAPTR|TestRoundTrip_NAPTR' -count=1
go test ./internal/zone/... -run 'TestParseDNSZone_SMIMEA|TestParseBIND_SMIMEA|TestRoundTrip_SMIMEA' -count=1
go test ./internal/zone/... -run 'TestParseDNSZone_LOC|TestParseBIND_LOC|TestRoundTrip_LOC' -count=1
go test ./internal/zone/... -run 'TestRoundTrip' -count=1
```

Then update the Per-Task Verification Map rows:

Row 10-01-01: Change command from `go test ./internal/zone/... -run TestSSHFP -count=1` to `go test ./internal/zone/... -run 'TestParseDNSZone_SSHFP|TestParseBIND_SSHFP|TestRoundTrip_SSHFP' -count=1`. Set File Exists to `YES`. Set Status to `GREEN`.

Row 10-01-02: Change command from `go test ./internal/zone/... -run TestNAPTR -count=1` to `go test ./internal/zone/... -run 'TestParseDNSZone_NAPTR|TestParseBIND_NAPTR|TestRoundTrip_NAPTR' -count=1`. Set File Exists to `YES`. Set Status to `GREEN`.

Row 10-01-03: Change command from `go test ./internal/zone/... -run TestSMIMEA -count=1` to `go test ./internal/zone/... -run 'TestParseDNSZone_SMIMEA|TestParseBIND_SMIMEA|TestRoundTrip_SMIMEA' -count=1`. Set File Exists to `YES`. Set Status to `GREEN`.

Row 10-01-04: Change command from `go test ./internal/zone/... -run TestLOC -count=1` to `go test ./internal/zone/... -run 'TestParseDNSZone_LOC|TestParseBIND_LOC|TestRoundTrip_LOC' -count=1`. Set File Exists to `YES`. Set Status to `GREEN`.

Row 10-02-01: Keep command `go test ./internal/zone/... -run TestRoundTrip -count=1` (already correct — prefix matches all TestRoundTrip_* tests). Set File Exists to `YES`. Set Status to `GREEN`.

Row 10-02-02: Change command from `go test ./... -run TestQuery -count=1` to manual-only. No TestQuery test exists in the zone package. The RRTYPE-08 behavior (NOERROR for missing record types) is verified through the server code path at server.go:790-793 and confirmed operational by Plan 03 SUMMARY. Update this row to mark it as manual-only with rationale: "No unit test — server-level behavior verified by integration testing and code review".

Update frontmatter:
- `status: complete`
- `nyquist_compliant: true`
- `wave_0_complete: true`

Check all Wave 0 requirement boxes and Validation Sign-Off boxes.

**Phase 11 VALIDATION.md fixes:**

First, run the corrected test command:
```bash
go test ./internal/resolver/... -run 'TestServeStale_BeyondMaxStaleTTL' -count=1
```

Then update the Per-Task Verification Map:

Row 11-03-03: Change command from `go test ./internal/resolver/... -run TestStaleWindow` to `go test ./internal/resolver/... -run TestServeStale_BeyondMaxStaleTTL -count=1`. Set Status to `GREEN`.

Set all other rows (11-01-01, 11-01-02, 11-02-01, 11-02-02, 11-03-01, 11-03-02) to Status `GREEN` after confirming their existing commands pass:
```bash
go test ./internal/resolver/... -run TestQNAMEMin -count=1
go test ./internal/cache/... -run TestNSEC -count=1
go test ./internal/cache/... -run TestNSEC3 -count=1
go test ./internal/resolver/... -run TestServeStale -count=1
```

Update frontmatter:
- `status: complete`
- `nyquist_compliant: true`
- `wave_0_complete: true`

Check all Wave 0 requirement boxes and Validation Sign-Off boxes.
  </action>
  <verify>
    <automated>cd /Users/ryan/development/dnsscienced && go test ./internal/zone/... -run 'TestParseDNSZone_SSHFP|TestParseBIND_SSHFP' -count=1 && go test ./internal/zone/... -run 'TestParseDNSZone_NAPTR|TestParseBIND_NAPTR' -count=1 && go test ./internal/zone/... -run 'TestParseDNSZone_SMIMEA|TestParseBIND_SMIMEA' -count=1 && go test ./internal/zone/... -run 'TestParseDNSZone_LOC|TestParseBIND_LOC' -count=1 && go test ./internal/zone/... -run TestRoundTrip -count=1 && go test ./internal/resolver/... -run TestServeStale_BeyondMaxStaleTTL -count=1 && grep 'nyquist_compliant: true' .planning/phases/10-record-type-expansion/10-VALIDATION.md && grep 'nyquist_compliant: true' .planning/phases/11-resolver-behaviors/11-VALIDATION.md && echo "PASS: Nyquist compliance achieved"</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'nyquist_compliant: true' .planning/phases/10-record-type-expansion/10-VALIDATION.md` returns 1 match
    - `grep 'nyquist_compliant: true' .planning/phases/11-resolver-behaviors/11-VALIDATION.md` returns 1 match
    - `grep 'TestSSHFP' .planning/phases/10-record-type-expansion/10-VALIDATION.md` returns 0 matches (old broken pattern removed)
    - `grep 'TestParseDNSZone_SSHFP' .planning/phases/10-record-type-expansion/10-VALIDATION.md` returns at least 1 match
    - `grep 'TestStaleWindow' .planning/phases/11-resolver-behaviors/11-VALIDATION.md` returns 0 matches (old broken pattern removed)
    - `grep 'TestServeStale_BeyondMaxStaleTTL' .planning/phases/11-resolver-behaviors/11-VALIDATION.md` returns at least 1 match
    - All test commands in the corrected VALIDATION.md files exit 0 when run
  </acceptance_criteria>
  <done>Both VALIDATION.md files have corrected test names, all rows green, nyquist_compliant: true in frontmatter</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan modifies only planning artifacts (.planning/ directory); no runtime code changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Tampering | VALIDATION.md test commands | accept | Planning artifacts only; incorrect test commands cause false Nyquist failures but no runtime impact; verified by running each command before updating status |
</threat_model>

<verification>
1. `go test ./internal/zone/... -count=1` passes (Phase 10 tests)
2. `go test ./internal/resolver/... -run TestServeStale -count=1` passes (Phase 11 stale tests)
3. `grep 'nyquist_compliant: true' .planning/phases/10-*/10-VALIDATION.md` returns match
4. `grep 'nyquist_compliant: true' .planning/phases/11-*/11-VALIDATION.md` returns match
5. `grep 'status: passed' .planning/phases/10-*/10-VERIFICATION.md` returns match
</verification>

<success_criteria>
- Phase 10 VERIFICATION.md shows passed (not human_needed)
- Phase 10 VALIDATION.md: all test commands match actual function names, all rows green, nyquist_compliant: true
- Phase 11 VALIDATION.md: TestStaleWindow replaced with TestServeStale_BeyondMaxStaleTTL, all rows green, nyquist_compliant: true
</success_criteria>

<output>
After completion, create `.planning/phases/14-v1.3-gap-closure/14-02-SUMMARY.md`
</output>
