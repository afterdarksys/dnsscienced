# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out

# Go workspace file
go.work

# Dependency directories
vendor/

# DNSScienced binaries
# Build these from source; they are never committed. deploy-zones.sh and
# deploy-dns.sh expect a locally built copy — see the build steps in README.
/dnsscienced
/dnsscienced-admin
/dnsscienced-compile
/dnsscienced-compile-darwin
/dnsscienced-compile-mac
/dnsscienced-roothints
/dnsscienced-log
/dnsscienced-test
/dnsscience-grpc
/dnscraft

# Build artifacts
/cmd/dnsscienced/dnsscienced
/cmd/dnsscienced-admin/dnsscienced-admin
/cmd/dnsscienced-compile/dnsscienced-compile
/cmd/dnsscienced-roothints/dnsscienced-roothints
/cmd/dnsscienced-log/dnsscienced-log
/cmd/dnsscience-grpc/dnsscience-grpc
/tools/dnscraft/dnscraft
/dnsasm/build/

# Compiled zone files
*.dzc

# Zone files directory (separate private repo)
zones/

# Log files
*.log

# Configuration files with secrets
*-local.yaml
*.key
*.pem
*.crt

# mTLS provisioning material (keys + certs) — never commit
grpc-certs/

# Local tool state and security reports (keep local)
.gstack/
.omc/

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~

# macOS
.DS_Store

# Linux compiled binaries
dnsscienced-linux
dnsscienced-linux-new
dnsscienced-admin-linux
dnsscienced-compile-linux-new
